# Nimbus Home Hub — Setup

The Nimbus Home Hub connects to 2.4 GHz Wi-Fi networks only; 5 GHz-only networks are not supported. Setup requires the Nimbus Companion app and a Nimbus account.

The Hub supports the Matter smart-home standard and can act as a Thread border router. Up to 64 Matter devices can be added to one Hub.
